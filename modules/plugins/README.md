# Plugin host module (placeholder)

Lua scripting host and stable C ABI for compiled feature modules. Lands in
Phase 5 — see `../../docs/ROADMAP.md`. Install-by-file only in v1, no
marketplace/registry infrastructure. No code yet.
