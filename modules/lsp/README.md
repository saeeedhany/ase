# LSP client module (placeholder)

JSON-RPC-over-stdio client giving diagnostics, completion, and
go-to-definition. Isolated as a separate process so a misbehaving language
server can never crash the editor. Lands in Phase 6 — see
`../../docs/ROADMAP.md`. No code yet.
